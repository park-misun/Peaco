# Peaco User Guide

GitHub Pages 배포용 정적 사이트 파일입니다.

1. `index.html`과 `.nojekyll` 파일을 GitHub 저장소의 최상위(root)에 업로드합니다.
2. GitHub → Settings → Pages에서 `Deploy from a branch`, `main`, `/(root)`를 선택합니다.
3. 배포 주소: https://park-misun.github.io/Peaco/
